x-uffizzi:
  ingress:
    service: app
    port: 8080

services:
  app:
    build:
      context: https://github.com/MydriveBot/Demo-vercel
      dockerfile: Dockerfile
    mysevice:
      env_file: ./config.env  
   
    deploy:
      resources:
        limits:
          memory: 500M
